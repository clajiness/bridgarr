<% diagnostic_sync_run_item = local_assigns[:sync_run_item] %>
<% diagnostic_path_options = { format: :text } %>
<% diagnostic_path_options[:sync_run_item_id] = diagnostic_sync_run_item.id if diagnostic_sync_run_item %>
<span
  class="inline-flex flex-wrap items-center gap-2"
  data-controller="clipboard"
  data-clipboard-report-value="<%= Base64.strict_encode64(Diagnostics::AssignmentReport.call(indexer_app: assignment, sync_run_item: diagnostic_sync_run_item)) %>">
  <button
    type="button"
    class="text-xs font-semibold text-slate-600 hover:text-slate-950 disabled:cursor-wait disabled:text-slate-400"
    data-clipboard-target="button"
    data-action="clipboard#copy">
    Copy diagnostic report
  </button>
  <%= link_to "Open diagnostic report",
    diagnostic_indexer_app_path(assignment, diagnostic_path_options),
    target: "_blank",
    rel: "noopener",
    hidden: true,
    class: "text-xs font-semibold text-blue-700 hover:text-blue-900",
    data: { clipboard_target: "fallback" } %>
  <span class="sr-only" role="status" aria-live="polite" data-clipboard-target="status"></span>
</span>
