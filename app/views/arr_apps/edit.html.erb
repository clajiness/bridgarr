<% content_for :title, "Editing arr app" %>

<div class="max-w-3xl space-y-6">
  <div>
    <h1 class="text-3xl font-semibold text-slate-950">Edit app</h1>
    <p class="mt-2 text-sm leading-6 text-slate-600"><%= @arr_app.name %></p>
  </div>

  <%= render "form", arr_app: @arr_app %>
</div>
