<% content_for :title, "New indexer" %>

<div class="max-w-3xl space-y-6">
  <div>
    <h1 class="text-3xl font-semibold text-slate-950">New indexer</h1>
    <p class="mt-2 text-sm leading-6 text-slate-600">Add a Jackett indexer record and choose which apps it belongs to.</p>
  </div>

  <%= render "form", indexer: @indexer %>
</div>
